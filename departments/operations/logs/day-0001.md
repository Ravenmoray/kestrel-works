# Day 1 of Year One — Operations Log

**Simulated date:** 2026-09-10
**Department run:** Research
**Real UTC timestamp (logged):** 2026-09-10T02:04:22Z

## Month theme used

September (Q1 — "Establish the operating rhythm"): "get the loop running
cleanly; small findings and tools, no ambition beyond reliability." Per
`departments/executive/decisions/2026-09-09-year-one-plan.md`.

## What was produced

Research (`research-lead`) picked up the roadmap's "Next" item and produced:

- `departments/research/findings/2026-09-10-project-scoping-reload-behavior.md`
  — investigates which Claude Code project-scoping mechanisms
  (`.claude/settings.json`/`settings.local.json` incl. hooks/permissions,
  `CLAUDE.md`, `.mcp.json`) reload live mid-session vs. require a restart,
  compared against `.claude/agents/`/`.claude/skills/`. Corrects the
  2026-09-09 finding: agents/skills reload live in general; the "frozen"
  behavior observed on day one was a documented one-time edge case (a
  brand-new directory that didn't exist at session start), not a standing
  limitation.

Flagged for other departments (recorded in the finding itself): the
workday loop no longer needs to treat persona/skill freezing as a standing
constraint; if `.claude/settings.json` is ever added it should apply live;
if an MCP server is ever wired up via `.mcp.json`, set it up before the
session starts rather than mid-session.

## Definition of done (per `departments/operations/CHARTER.md`)

- [x] Department's output exists on disk — verified directly (`ls`, `wc`,
      read the file).
- [x] `HISTORY.md` has a new entry — "Day 1 of Year One — Research" section
      prepended.
- [x] `website/data/updates.json` has a matching entry — Research entry
      dated 2026-09-10 prepended.
- [x] Roadmap updated — removed the completed "Next" item, added it under
      "Shipped".
- [x] Site republished/verified — see below.
- [x] Operations log recorded (this file).

## Site check

Started `./scripts/serve.sh 8765` in the background, curled both target
URLs, then stopped the server:

- `http://localhost:8765/website/index.html` → `200`
- `http://localhost:8765/website/data/updates.json` → `200`

Both passed. Confirmed no leftover `http.server` process after stopping.
